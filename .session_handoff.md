---
name: 2026-04-28 handoff — textbook started, SC sweep parked
description: User switched from Intel iMac to Mac mini M4 mid-session. Textbook has plan + Ch 1/2/3 outlines; next chapter to write is Ch 11 or Ch 18; SC e2e re-sweep was killed at 555/2019; remaining to-do ordering.
type: project
originSessionId: ea7a95b5-58d7-4678-b779-7b72c9d0cab3
---
## Where work stopped on 2026-04-28 ~5:55 AM (Intel iMac)

User said `我要去上班 要換電腦了` — switching to the M4 Mac mini.
Killed the running `run_vkglcts_to_sc.py` sweep (was at 555/2019, bp=19,
bad=536; ~33 min in, ETA was ~87 min more — so ~120 min total).
Partial TSV at `/tmp/sc_e2e_v2.tsv` is on the **Intel iMac's /tmp**,
not on the shared exFAT volume. M4 won't see it. Ignore it on resume.

## What's done in this session (in order)

1. **Sprint 59** — VK-GL-CTS → SystemC E2E pipeline plumbing
   (atexit hook in glcompat, payload widening, SC_CPLUSPLUS=201703L
   workaround, run_vkglcts_to_sc.py driver). 14 bit-perfect across 2019
   cases.
2. **Sprint 60** — `SceneOp::CLEAR` carries scissor rect + colorMask
   lane. `color_clear.*` 8/19 → 19/19 bit-perfect.
3. **Sprint 61** — multi-varying capture + full blend state +
   per-batch viewport in scene format (incl. `vp_x/y` add to
   `ScToPaAdapterCa`). Blend median RMSE 110 → 6.2 on a 50-case
   sample.
4. **PROGRESS.md slim** — 1697 → 111 lines, kept only to-do.
5. **Started textbook** at `textbook/`:
   - `plan.md` — six-part / ~22-chapter structure + pedagogical
     strategy (chinese narrative + english tech terms).
   - `ch01_why_a_gpu.md` — scope freeze + 3 zoom block diagrams + repo
     tour. **Includes a clarification that "ISA bytecode + DrawState +
     buffers" is in-process struct, NOT a file; `.scene` file is a
     separate post-VS snapshot without bytecode.**
   - `ch02_isa.md` — 64-bit instruction format walk, 6 sub-sections,
     decisions log incl. the s2-no-class-field call.
   - `ch03_simulation_strategy.md` — three reference layers
     (sw_ref / SC LT / SC CA) + scene record/replay + 3-backend
     triangle proof.

## What's pending — pick up from any of these

User had open todo list; in priority order:

1. **Slim regress_report.md** to just sw_ref + SC pass/fail per group.
   - User asked, sweep was launched to refresh numbers, sweep got
     killed at 555/2019 — so SC numbers in `regress_report.md` are
     still Sprint 59 era.
   - Re-launch with `tools/run_vkglcts_to_sc.py --cases-file
     /tmp/sw_pass_cases.txt --tsv ...` (case list is on shared
     volume nowhere; need to regen from a sw_ref sweep first if not
     present on M4).
   - Aggregator now has `--top-level` flag for one-line-per-group
     compact form. Use that.
2. **Continue textbook outlines.** Three ready (Ch 1/2/3). Next
   suggested:
   - Ch 11 (compiler under stress) — most likely to stress the
     6-section template; deep cross-sprint bug story.
   - Ch 18 (SC parity track) — picks up where the sprint 59-61 work
     ends; reader leaves running `run_vkglcts_to_sc.py`.
3. **Sample chapter prose.** Expand any one outline to ~8000 words to
   set chapter density / style. User hasn't asked for this yet.

## What to say to resume

User can drop into a fresh session on M4 with any of:

- `從 textbook 那邊繼續寫 Ch 11` — go to compiler-under-stress chapter outline.
- `從 textbook 那邊繼續寫 Ch 18` — go to SC parity track chapter outline.
- `把 regress_report 改成 sw + SC pass/fail` — relaunch sweep (~2 hr
  on 4 workers / 15 s timeout) and slim the report.
- `把 ch01 的 ISA bytecode + DrawState in-memory 那段 expand 成完整
  prose` — sample chapter test.

## M4 environment reminders (already documented in project_dual_mac_setup.md)

- Run `chmod +x tools/*.sh ci/*.sh` after first checkout (exFAT
  doesn't preserve exec bit).
- `./tools/setup_env.sh --bootstrap-venv` to set up
  `~/.local/share/gpu-venv/arm64/`.
- `source .venv/activate` works on both arches via the dispatcher
  stub.
- M4's CLT should be fresh — no need for the `SC_CPLUSPLUS=201703L`
  workaround? Actually the SystemC install on M4 might also be C++17
  compiled. Re-check with `nm -gU $(brew --prefix systemc)/lib/libsystemc.dylib
  | grep sc_api_version`.
- M4's Homebrew is at `/opt/homebrew`, not `/usr/local`. setup_env.sh
  picks the right prefix from `uname -m`.
- Building for arm64 means `build-arm64/` and
  `build_vkglcts-arm64/` (per-arch build dirs already wired).

## Files touched this session that are on the shared exFAT volume

(All accessible from M4 immediately.)

| file | purpose |
|---|---|
| `glcompat/src/glcompat_render.cpp` | Sprint 59-61 scene format + CLEAR scissor + multi-varying + viewport + blend state |
| `glcompat/src/glcompat_state.cpp` | Sprint 59 atexit + Sprint 60 scissor rect record |
| `glcompat/src/glcompat_es2.cpp` | Sprint 61 multi-varying VS capture |
| `glcompat/src/glcompat_runtime.h` | Sprint 60-61 API declarations |
| `tests/conformance/sc_pattern_runner.cpp` | Sprint 59-61 parser + replay extensions |
| `systemc/blocks/adapters/{include,src}/sc_to_pa_adapter_ca.{h,cpp}` | Sprint 61 vp_x/vp_y forward |
| `systemc/common/include/gpu_systemc/payload.h` | Sprint 59 array size widen |
| `systemc/CMakeLists.txt` | Sprint 59 SC_CPLUSPLUS=201703L |
| `tools/run_vkglcts_to_sc.py` | E2E driver (cases-file, workers, tsv, resume, viewport-aware crop) |
| `tools/aggregate_sc_e2e.py` | TSV → markdown (now with `--top-level`) |
| `docs/regress_report.md` | Sprint 59-61 sections (still has Sprint 59-era SC sweep numbers, awaiting refresh) |
| `docs/PROGRESS.md` | Slimmed to to-do only (111 lines) |
| `textbook/plan.md` | Textbook plan |
| `textbook/ch01_why_a_gpu.md` / `ch02_isa.md` / `ch03_simulation_strategy.md` | Three chapter outlines |

## In-tree tests stable as of session end

`ctest --test-dir build-x86_64 -j4`: **56/56 PASS** with
`-DGPU_BUILD_SYSTEMC=ON`. Sprint 60/61 changes did not regress any
existing test. Re-verify on M4 after first build.
