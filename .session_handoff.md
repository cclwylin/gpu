---
name: 2026-04-28 PM handoff — git rescue + slim regress + Ch 11/18 + SC sweep ROI confirmed
description: M4 Mac mini session. Rescued 8 commits worth of uncommitted Sprint 36-61 work + chmod cleanup, pushed to origin. Wrote textbook Ch 11 / 18 outlines. Slimmed regress_report 951→199 with fragment_ops breakdown. SC sweep at 30s timeout confirmed depth_stencil/random/interaction subgroups are NOT budget-bound — they need actual chain work. Next ROI is the rgb_func_alpha_func chain fix.
type: project
originSessionId: 69316d31-6579-476f-8dc8-84c7fe7ba7da
---

## Where work stopped on 2026-04-28 PM (M4 Mac mini)

User said `handoff 起來`. SC sweep at 30s timeout had been killed mid-run
(525 / 1985, see § 30s sweep finding). Working tree clean, all commits
pushed.

## What's done in this session (in order)

### 1 — Git rescue + chmod cleanup (commits `fd51503` → `1de0d15`)

The M4 startup found 298 modified-or-untracked files going back to
Sprint 36, none of them committed. Split into 8 logical commits:

| commit    | scope                                                          | files |
|---|---|---:|
| `fd51503` | gitignore: third-party VK-GL-CTS / glmark2 / mesa-demos trees  |   1 |
| `3f50a54` | rescue untracked: SC LT models, glcompat_es2, scenes, textbook |  58 |
| `0b8e00a` | chmod: 100644 → 100755 squash for exFAT-induced exec-bit noise | 190 |
| `05bc0c6` | compiler: GLSL parser + codegen rewrite (Sprint 47-58)         |   6 |
| `81d0f56` | sw_ref + glcompat: fragment_ops state + correctness            |  11 |
| `f6d63a7` | sc-chain: scene format + Sprint 59-61 plumbing                 |  19 |
| `c3562bb` | docs + build: PROGRESS slim, microarch refresh, top-level CMake|  21 |
| `40d3e78` | systemc/blocks README: CA class column                         |   1 |
| `75adb06` | textbook: Ch 11 + Ch 18 outlines                               |   2 |
| `cf81ea1` | docs/regress_report: slim 951 → 137 lines                      |   1 |
| `e786a9d` | docs/regress_report: drill fragment_ops by 2nd/3rd-level (+62) |   1 |
| `1de0d15` | systemc/CMakeLists: brew SystemC 3.0.x fallback (Sprint 62)    |   1 |

`origin/main` at `1de0d15`. Working tree clean.

### 2 — textbook Ch 11 + Ch 18 outlines (`75adb06`)

Both follow the 6-section template (Goal / Concepts / Code walk /
Hands-on / Decisions log / Exercises) consistent with Ch 1-3.

- `textbook/ch11_compiler_under_stress.md` (282 lines) — walks four
  random-shader bugs in diagnosis order: `bool(x)` identity-pass →
  VS/FS c-bank slot collision → c-bank 16→32 widen (5-file change) →
  constant-depth boundary precision. Chapter-end target:
  `fragment_ops.* 1887/1923 (98.1 %)` (matches `regress_report.md`).

- `textbook/ch18_sc_parity_track.md` (282 lines) — picks up Sprint 59-61:
  atexit hook trick, `sc_pattern_runner` replay, `run_vkglcts_to_sc.py`
  driver orchestration, three follow-up gaps (CLEAR scissor / blend /
  multi-varying). Chapter-end target: `bp=19 / 1985` (which is what
  Sprint 60-61 closed for `color_clear`; the broader sweep numbers
  are below in § 30s sweep finding).

Plan-pending chapters from `textbook/plan.md`: Ch 4-10 (sw_ref +
compiler internals), Ch 12-17 (SystemC + scene format), Ch 19-20
(toward RTL / driver), and the appendices.

### 3 — `docs/regress_report.md` slim (Sprint 56 history → live status)

`cf81ea1` collapsed the 951-line per-sprint history into a 137-line
"current pass/fail per group" report. `e786a9d` then added 62 lines of
`fragment_ops.*` breakdown (§ Sprint 62 SC sweep below produced the data).

Now structured as:

- 4-row top metric block
- VK-GL-CTS per-group table with sw_ref AND SC parity columns side by side
- `fragment_ops.*` 1887-case 2nd-level subgroup breakdown + `blend.*` 3rd-level
- 49 GL example regress as a status histogram
- Sprint 42 → 61 history collapsed to a 7-row trend table
- Sweep run notes pinning the snapshot to M4 mini

Per-sprint long-form sections moved out — they're in git log + textbook.

### 4 — Sprint 62 SC sweep + ROI mining

Two SC sweeps run this session:

**15s timeout sweep** (`/tmp/sc_e2e_v2.tsv`, ~85 min on M4 mini, 4 workers):
- bp = 80 / 1985 (up from 14 at Sprint 59)
- timeout = 870, diff (RMSE > 0) = 1016
- `color_clear` 19/19 bit-perfect (Sprint 60 closed)

**30s timeout sweep** (`/tmp/sc_e2e_v3.tsv`, killed at 525 / 1985):
- **Confirmed timeout-lift hypothesis is wrong** for depth_stencil
- 0 / 417 `fragment_ops.depth_stencil.*` cases finished within 30s
  (same as 15s — they're not "almost done", they genuinely need the
  chain to be faster or the case to be skipped)
- ETA was 171 min remaining at 525 cases vs 50 min for the 15s
  sweep at the same point — pure overhead with no data uplift
- Killed early to save ~3 hr; partial TSV is on M4's `/tmp` and lost
  on next reboot — re-derive numbers from `v2.tsv` if needed

Drilling `fragment_ops` from the 15s sweep gave the actionable map
(now in regress_report.md):

| `fragment_ops.<sub>`     | total | bp | diff | timeout | mean RMSE |
|---|---:|---:|---:|---:|---:|
| `blend`                  | 1060  | 54 | 1006 |   0     | 58.91 |
| `depth_stencil`          |  621  |  0 |    0 |  621    | —     |
| `random`                 |  100  |  0 |    0 |  100    | —     |
| `interaction.basic_shader`| 64   |  0 |    0 |   64    | —     |
| `stencil`                |   17  |  0 |    0 |   17    | —     |
| `scissor`                |   17  |  7 |    8 |    2    | 94.76 |
| `depth`                  |    8  |  0 |    0 |    8    | —     |

`blend.<3rd>` breakdown:

| `blend.<sub>`                 | total | bp | diff | RMSE range  |
|---|---:|---:|---:|---|
| `equation_src_func_dst_func`  |  630  | 54 | 576  | 0.5–82.7    |
| **`rgb_func_alpha_func`**     |  421  |  0 | 421  | **59.9–170**|
| `rgb_equation_alpha_equation` |    9  |  0 |   9  | 3.3–42.3    |

→ The 421 `rgb_func_alpha_func` cases are the next obvious ROI:
Sprint 61 wired separate-RGB / alpha blend factors into `glcompat`'s
scene capture, but the corresponding lane in
`sc_to_pa_adapter_ca` + `perfragmentops_lt` hasn't been fed through.
Adding those lanes is the highest-leverage SC chain fix on the table.

## What's pending — pick up from any of these

In rough ROI order:

1. **Wire separate-RGB / alpha blend factors into the SC chain.**
   `systemc/blocks/adapters/.../sc_to_pa_adapter_ca.{h,cpp}` already
   has `vp_x/y` (Sprint 61) — add the parallel set of fields for
   `blend_src_rgb / blend_src_alpha / blend_dst_rgb / blend_dst_alpha
   + blend_equation_rgb / blend_equation_alpha + blend_color`. Then
   wire them through `perfragmentops_lt`'s blend computation.
   Expected: `blend.rgb_func_alpha_func` 421 cases drop from RMSE
   60–170 to < 5; the `equation_src_func_dst_func` band's bottom-50
   (closest to bp) likely flip too.
   Re-run sweep after with `tools/run_vkglcts_to_sc.py
   --cases-file /tmp/sw_pass_cases.txt --workers 4 --sc-timeout 15
   --tsv /tmp/sc_e2e_v4.tsv`.

2. **Mine `equation_src_func_dst_func` 576 RMSE-but-not-bp cases for
   ULP-class fixes.** Sort them by RMSE ascending; the bottom 50 are
   visually identical to bp. One root cause typically unlocks a band
   of 30+. Looks for: `BF_SRC_ALPHA_SATURATE` rounding mode, blend
   color literal interning, alpha-channel saturation order.

3. **Decide what to do about the 810 timeout cases.** Three honest
   options:
   (a) profile `sc_pattern_runner` on a `depth_stencil.stencil_ops.*`
       case — find what's actually slow (probably stencil-op replay
       loop); maybe 10x speedup is achievable
   (b) accept those subgroups as out-of-scope for SC parity and
       document it in regress_report.md
   (c) per-group adaptive timeout (15s for blend / scissor, 60s for
       depth_stencil — needs change to `run_vkglcts_to_sc.py`)

4. **Continue textbook outlines.** Plan has Ch 4-10 (sw_ref +
   compiler), Ch 12-17 (SystemC + scene), Ch 19-20 (sketches), 3
   appendices. ROI ordering would put Ch 4 (vertex pipeline), Ch 6
   (per-fragment ops), Ch 14 (CA chain) next — all have working
   ground truth in the repo already.

5. **Sample chapter prose.** Expand any one outline (ch01_why_a_gpu
   is the best candidate — most polished outline, broadest audience)
   to ~8000 words. User hasn't asked yet; first such expansion sets
   the chapter density.

## What to say to resume

User can drop into a fresh session with any of:

- `修 SC chain rgb_func_alpha_func` — go to ROI #1, separate-RGB/alpha
  blend lane in adapter + PFO_lt.
- `mine equation 576 RMSE 看 bottom 50 共同 root cause` — go to ROI #2.
- `從 textbook 那邊繼續寫 Ch 4` — go to vertex pipeline outline.
- `把 ch11 expand 成 prose` — expand the 282-line outline to ~8000-word
  full prose; sets chapter density bar for the rest.

## M4 environment reminders

- **No GCC** on this M4 (handoff installer wants `g++-15`); we're using
  AppleClang 21 from CLT. Builds cleanly because Sprint 62's brew
  SystemC fallback (`systemc/CMakeLists.txt:38-60`) makes the standard
  `find_package(SystemCLanguage)` path non-required.
- Build dirs are `build-arm64/` (libs + sc_pattern_runner) and
  `build_vkglcts-arm64/` (deqp-gles2). The `build_vkglcts.sh` driver
  takes `MAIN_BUILD=$(pwd)/build-arm64` env var to re-target.
- All commits are on `origin/main` (cclwylin/gpu) at `1de0d15`. SSH is
  not configured on M4; remote uses HTTPS via `gh auth setup-git`.
- exFAT volume keeps flipping mode 100644 → 100755 every checkout.
  Sprint 62 squashed 190 of those into `0b8e00a`; new ones will appear
  again next time you build.

## Files touched this session that are on the shared exFAT volume

(All accessible from Intel iMac too.)

| file                                       | purpose                                                    |
|---|---|
| `.gitignore`                               | tighten third-party VK-GL-CTS / glmark2 / mesa-demos       |
| `.session_handoff.md`                      | this file                                                  |
| `docs/regress_report.md`                   | slimmed; live sw_ref + SC parity status                    |
| `systemc/CMakeLists.txt`                   | Sprint 62 brew SystemC fallback                            |
| `textbook/ch11_compiler_under_stress.md`   | new chapter outline                                        |
| `textbook/ch18_sc_parity_track.md`         | new chapter outline                                        |

## In-tree tests stable as of session end

`ctest --test-dir build-arm64 -j4`: **56/56 PASS** with `-DGPU_BUILD_SYSTEMC=ON`.
Verify on either Mac after first build:

```sh
./tools/setup_env.sh --bootstrap-venv     # one-shot per host
source .venv/activate
./tools/setup_env.sh --configure
cmake --build "$BUILD_DIR" -j4
ctest --test-dir "$BUILD_DIR" -j4         # expect 56/56
./tools/build_vkglcts.sh                  # reuses MAIN_BUILD
python tools/run_vkglcts_sweep.py --bin "$VKGLCTS_BUILD_DIR/modules/gles2/deqp-gles2"
# expect 2035/2143 (95.0 %) sw_ref pass
```

## Useful artefacts left on M4 `/tmp` (will not survive reboot)

| path                          | size  | purpose                                        |
|---|---|---|
| `/tmp/sc_e2e_v2.tsv`          |  ~80 KB | 15s-timeout SC sweep, complete 1985 / 1985    |
| `/tmp/sc_e2e_v3.tsv`          |  ~25 KB | 30s-timeout SC sweep, partial 525 / 1985 (killed) |
| `/tmp/sw_pass_cases.txt`      |  ~80 KB | 1985 sw_ref-passing case suffixes             |

Re-derive on Intel iMac with the hands-on snippet above + the case-list
gen loop in this session's transcript (`for grp in color_clear ...`).
