// Placeholder TU. All math primitives are header-only in include/gpu/types.h.
// This file exists so the static lib has at least one TU per directory and
// to host any non-trivial math helpers added later.

namespace gpu {
[[maybe_unused]] inline void _types_anchor() {}
}
